import { HOST } from "@/apiConfig";

const API = {
  uploadImg: `${HOST}/test/img`,
  uploadImg2: `${HOST}/test/img2`,
  uploadPdf: `${HOST}/test/pdf`,
  uploadFile: `${HOST}/test/file`,
};

export default API;
